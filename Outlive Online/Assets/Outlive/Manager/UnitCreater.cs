using System.Collections;
using System.Collections.Generic;
using Photon.Pun;
using UnityEngine;

public class UnitCreater : MonoBehaviourPunCallbacks
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    // [PunRPC]
    // public bool TryCreate(string unitName, string playerName, Vector3 coordenates, out GameObject createdObject)
    // {
        
    // }

    // private void Create(string unitName, string playerName, Vector3 coordenates)
    // {

    // }
}
